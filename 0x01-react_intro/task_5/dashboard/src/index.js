import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css'; // Sample CSS file

const App = () => {
    return <h1>Hello, React with Webpack!</h1>;
};

ReactDOM.render(<App />, document.getElementById('root'));
